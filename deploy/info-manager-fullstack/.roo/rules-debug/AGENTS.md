# Project Debug Rules (Non-Obvious Only)

- **API 資料庫連線**: 在本地開發時，如果 `MONGODB_URI` 未設定，API 伺服器會發出警告並繼續運行，但所有資料庫操作將失敗並返回空陣列或 500 錯誤。這是預期的行為，用於允許在沒有資料庫連線的情況下進行前端開發。
- **加密金鑰**: 如果 `ENCRYPTION_KEY` 未設定，API 伺服器將無法啟動。請務必在 `.env` 檔案中設定此變數以進行本地開發和除錯。